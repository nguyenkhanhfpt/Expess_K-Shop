extends ../layout/masterLayout
block content
    .container.pt-5
        .row
            .col-4
                img(src=`http://localhost:3000/images/${product.imageProduct}`, alt="", style="width: 100%")

            .col-8
                h2= product.nameProduct
                p= product.price.toLocaleString() + ' vnd'
                a.btn.btn-primary(href='#') Add to cart